load("//:defs.bzl", "trill_rust_crate")

trill_rust_crate(
    name = "exec-server",
    crate_name = "trill_exec_server",
    integration_deps_extra = ["//codex-rs/exec-server/tests/common:common"],
    test_tags = ["no-sandbox"],
    extra_binaries = [
        "//codex-rs/cli:codex",
    ],
)
